name: traffic_project
version: "1.0"
profile: default

config-version: 2

model-paths: ["models"]


models:
  traffic_project:

    staging:
      +schema: staging
      +materialized: view

    marts:
      +schema: marts
      +materialized: view 

      fct_sinistros:
        +materialized: table 

      fct_pessoas_sinistro:
        +materialized: table 

